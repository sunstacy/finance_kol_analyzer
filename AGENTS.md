# AGENTS.md

## Cursor Cloud specific instructions

This repository (`finance_kol_analyzer`) is currently an empty/initialized project with no application code, dependencies, or services. The update script is a no-op (`true`) since there are no dependencies to install.

When application code is added, update this section and the VM update script accordingly with:
- How to install dependencies (e.g. `pip install -r requirements.txt`, `npm install`)
- How to run lint, tests, and the dev server
- Any non-obvious environment setup caveats
